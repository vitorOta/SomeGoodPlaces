<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/colorPrimaryVariant</item>

        <item name="fontFamily">@font/open_sans</item>
        <item name="android:fontFamily">@font/open_sans</item>

        <item name="ratingBarStyle">@style/AppTheme.RatingBarStyle</item>
    </style>

    <style name="AppTheme.RatingBarStyle" parent="Widget.AppCompat.RatingBar.Small">
        <!--        <item name="android:progressDrawable">@drawable/rating_progress_drawable</item>-->
        <item name="android:stepSize">1</item>
    </style>

    <style name="AppTheme.ToolbarTextAppearance">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="AppTheme.ToolbarTextAppearance.Expanded">
        <item name="android:textSize">32sp</item>
    </style>
</resources>